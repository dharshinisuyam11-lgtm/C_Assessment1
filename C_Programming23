#include <stdio.h>
int main()
{
    int a, result, sum;
    printf("enter the number:");
    scanf("%d", &a);
    result = a - 5;
    sum = (result / 10) + (result % 10);   // for a 2-digit result
    (sum % 2) && printf("%d", result);
    return 0;
}
