#include <stdio.h>
int main()
{
 int a, ones, tens, hun, reverse;
 printf("Enter a three digit number: ");
 scanf("%d", &a);
 ones=a%10;
 tens=(a/10)%10;
 hun=a/100;
 reverse=ones*100+tens*10+hun;
 printf("%d",reverse);
 return 0;
}
